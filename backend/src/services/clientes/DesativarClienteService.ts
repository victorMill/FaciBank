// import { IClienteRepository } from "../../repositories/IClienteRepository";

// export class DesativarClienteService {
//     constructor(private readonly repo: IClienteRepository) {}

//     execute(id: number) {
//         const cliente = await this.repo.findById(id)

//         if (cliente.conta)
//     }
// }
